<?php
// ================================================================
// FILE 3: routes/channels.php — WebSocket channel authorize
// Yeh file already exist karti hai — isme yeh add karo
// ================================================================
// use Illuminate\Support\Facades\Broadcast;
//
// // Private channel — sirf logged in user apna channel access kar sake
// Broadcast::channel('user.{id}', function ($user, $id) {
//     return (int) $user->id === (int) $id;
// });
 